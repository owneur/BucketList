<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path('home') }}"><img src="{{ asset('images/logo.png') }}" alt="une image d'un sceau"></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('home') }}">Bucket-List</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('wishes') }}">List</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('create') }}">Add wish</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('aboutUs') }}">About us</a>
            </li>
        </ul>
    </div>
</nav>
{# read and display all flash messages #}
{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ label }}">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}
